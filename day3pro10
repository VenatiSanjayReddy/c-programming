#include <stdio.h>
#define for1to10 for(int i=1; i<=10; i++)

int main()
{
   int num = 4, x;

   for1to10
   {
      x = num+i;
      printf("%d \n", x);
   }

   printf("\nMultiplication table of %d: \n", num);
   for1to10
   {
      printf("%d \n", num*i);
   }

   return 0;
}
